/** Route group for business dashboard (`/business/*`); keeps URLs unchanged. */
export default function BusinessRouteGroupLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children
}
